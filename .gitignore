# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# generated at build (next.config.ts) — the PWA update-check version stamp
public/version.json

# claude code (local dev only)
.claude/

# Python
__pycache__/
*.pyc

# Swift build output (the Ask-MLR Foundation Models service — WIP, never deployed)
media-server/fm-service/.build/
media-server/embed-service/.build/

# Native iOS app — moved to its own repo (btheis15/mlr-app-ios).
# Guard against Xcode re-writing state files here while a stale project is open.
/ios/
media-server/.mod-recheck.json
